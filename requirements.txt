# SVEN Extension - Requirements
# 
# ⚠️ SPLIT REQUIREMENTS TO AVOID CONFLICTS
#
# Choose based on your role:
#
# Rohit (Partner 1 - Model/Training):
#   pip install -r requirements/training.txt
#
# Kush (Partner 2 - Data/Evaluation/Webapp):
#   pip install -r requirements/evaluation.txt
#
# For full development (both partners' dependencies):
#   pip install -r requirements/training.txt -r requirements/evaluation.txt
#
# Docker builds install all dependencies automatically.

# Base requirements (everyone needs these)
-r requirements/base.txt

# Optional: Install all dependencies
# Uncomment if you want everything:
# -r requirements/training.txt
# -r requirements/evaluation.txt
